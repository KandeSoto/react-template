export interface IconIonicProps {
    name: string; // nombre del icono (por ejemplo, "home", "person", etc.)
    color?: string; // opcional: color del icono
    size?: string; // opcional: tamaño (small | large | default)
    className?: string; // opcional: clases personalizadas
}
